#include<stdio.h>
void main()
{
    int a,i;
    printf("Enter a no:");
    scanf("%d",&a);
    if(a%2==0)
    for(i=1;i<=10;i++)
    printf(" %d x %d = %d\n",i,a,i*a);
    else
    for(i=1;i<=15;i++)
    printf(" %d x %d = %d\n",i,a,i*a);
}
