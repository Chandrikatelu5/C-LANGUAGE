#include<stdio.h>
int add(int a,int b);
void main()
{
    int a,b;
    printf("Enter two no:");
    scanf("%d%d",&a,&b);
    printf("addition of two no:%d",add(a,b));
}
int add(int a,int b)
{
    return a+b;
}
