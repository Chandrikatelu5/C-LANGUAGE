#include <stdio.h>
void prime(int a);
void main()
{
    int a,i,count=0;
    printf("Enter any number:");
    scanf("%d",&a);
    prime(a);
}
void prime(int a)
{
    int i,count=0;
    for(i=2;i<a;i++)
    {
        if(a%i==0)
        count++;
    }
    if(count==0)
    printf("prime");
    else
    printf("composite");
}
